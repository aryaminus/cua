# ControlKeel managed (do not edit) - artifacts that must not be committed
/controlkeel/
/.controlkeel/
/.agents/skills/
# End ControlKeel managed

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Tool session state (never commit)
.commandcode/
.opencode/

# Secrets
.env
.env.*
!.env.example

# Evidence: commit curated runs referenced by README/REPORT; ignore bulky scratch
evidence/scratch/
evidence/performance/scratch/

# Operator session state
*.operator-session.json

# Operator model slice from `cua vet-guard` (see cua/openrouter.py): additive-only,
# additive-only protective gate, refreshed upstream; never hand-edit its internals.
